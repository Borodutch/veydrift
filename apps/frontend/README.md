# @veydrift/frontend

Preact + TypeScript + Tailwind public web app placeholder.

## Scripts

- `pnpm dev` — start dev server
- `pnpm build` — build for production
- `pnpm preview` — preview production build
