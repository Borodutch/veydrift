{
  "name": "@veydrift/frontend",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.1.42",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:playable": "(node scripts/generate-image-variants.mjs || bun scripts/generate-image-variants.mjs) && vite build --mode playable",
    "build:test": "(node scripts/generate-image-variants.mjs || bun scripts/generate-image-variants.mjs) && vite build --mode settlement",
    "check": "tsc --project tsconfig.json && vite build && vite build --mode settlement && node ../../scripts/veydrift-test-frontend-config-check.mjs --dist dist --api-url https://api-test.veydrift.com",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "node scripts/generate-image-variants.mjs || bun scripts/generate-image-variants.mjs",
    "precheck": "node scripts/generate-image-variants.mjs || bun scripts/generate-image-variants.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "serve": "bun scripts/serve.mjs",
    "start": "bun run serve",
    "test": "bun test tests src/*.test.ts",
    "test:touch-browser": "node --test tests/planetPickerTouchBrowser.browser.mjs"
  },
  "dependencies": {
    "@farcaster/miniapp-sdk": "^0.3.0",
    "@veydrift/universe": "workspace:*",
    "lucide-preact": "^1.16.0",
    "preact": "^10.27.2",
    "viem": "^2.52.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "autoprefixer": "^10.4.21",
    "playwright-core": "^1.62.1",
    "postcss": "^8.5.6",
    "sharp": "^0.34.1",
    "tailwindcss": "^3.4.18",
    "typescript": "^5.9.3",
    "vite": "^5.4.21"
  }
}
