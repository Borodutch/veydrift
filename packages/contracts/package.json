{
  "name": "@veydrift/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "fmt": "forge fmt",
    "fmt:check": "forge fmt --check",
    "check": "bun run check:fast && bun run check:storage && bun run manifest:check",
    "check:fast": "forge fmt --check && bun run check:upgrade-sizes",
    "check:upgrade-sizes": "forge build src/VeydriftGame.sol src/VeydriftMoonSystem.sol src/VeydriftCombatModule.sol src/VeydriftGameplayModule.sol src/VeydriftPlanetManagementModule.sol src/VeydriftAttackProtectionModule.sol src/VeydriftAcsAttackModule.sol src/VeydriftColonizationModule.sol src/VeydriftDefenseHoldModule.sol src/VeydriftStateMigrationModule.sol src/VeydriftFirstPlanetSettlementModule.sol --sizes --skip test --skip script",
    "check:storage": "FOUNDRY_OPTIMIZER=false FOUNDRY_VIA_IR=true forge build --force --extra-output storageLayout --skip test --skip script && node scripts/check-storage-layout.mjs",
    "storage:check": "bun run check:storage",
    "test": "node scripts/run-tests-separately.mjs",
    "manifest:generate": "VEYDRIFT_SOURCE_COMMIT=$(git log -1 --format=%H -- . ':(exclude)manifests/vey-741-base-fork-dry-run.json') VEYDRIFT_MANIFEST_PATH=manifests/vey-741-base-fork-dry-run.json forge test --match-path test/VeydriftUniswapLaunchMainnetFork.t.sol -vv",
    "manifest:check": "node scripts/validate-veydrift-uniswap-launch-manifest.mjs && bun run manifest:test",
    "manifest:test": "node --test scripts/validate-veydrift-uniswap-launch-manifest.test.mjs"
  },
  "dependencies": {
    "viem": "^2.52.0"
  }
}
