[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build:frontend"]

[start]
cmd = "cd apps/frontend && bun run preview -- --port 80"

[variables]
NODE_ENV = "production"
